# SPA на BrowserRouter: без этого правила любой адрес, кроме корня, отдаёт 404 —
# файла с таким путём в сборке нет. То же самое, что редиректы в netlify.toml,
# но в формате, который читают Cloudflare Pages: netlify.toml они игнорируют.
#
# Отдельного правила для /storybook не нужно: существующие файлы отдаются
# раньше этого перехвата, поэтому статика библиотеки компонентов не страдает.
/*    /index.html   200
