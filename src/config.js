// Переключатели незавершённых решений. Держим отдельно от кода экранов, чтобы
// вернуть прежний вид можно было одной правкой, без git-операций.

// Шапка профиля: центрированная композиция без 3D-линии (в разработке).
// false — прежняя шапка с волной, выровненная по левому краю.
export const PROFILE_V2 = true;

// Версия приложения: показывается на онбординге и попадает в технические
// детали ошибки авторизации — держим в одном месте, чтобы не разъезжались.
export const APP_VERSION = 'v2.8.2';
